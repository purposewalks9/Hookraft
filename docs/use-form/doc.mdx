## Installation

<InstallationTabs item="@hookraft/useform">
```bash
pnpm add @hookraft/useform
```
</InstallationTabs>

---

## useForm

```tsx
import { useForm } from "@hookraft/useform"
```
```tsx
const form = useForm({
  fields: {
    email: {
      value: "",
      rules: [
        { required: true, message: "Email is required" },
        { pattern: /^\S+@\S+\.\S+$/, message: "Invalid email address" },
      ],
    },
    password: {
      value: "",
      rules: [
        { required: true, message: "Password is required" },
        { minLength: 8, message: "Must be at least 8 characters" },
      ],
    },
  },
  onSubmit: async (values) => await loginUser(values),
  onSuccess: () => console.log("Logged in!"),
  onError: (err) => console.error("Failed:", err),
})
```

### The problem it solves

Every form in React makes you write the same boilerplate — field state, error state, loading state, validation logic, submit handling.

```tsx
// Without useForm
const [email, setEmail] = useState("")
const [password, setPassword] = useState("")
const [isSubmitting, setIsSubmitting] = useState(false)

// With useForm
const form = useForm({
  fields: {
    email: { value: "", rules: [{ required: true }] },
    password: { value: "", rules: [{ required: true }] },
  },
})
```

### How it works

Each field exposes everything you need directly:

```tsx
form.fields.email.value
form.fields.email.error
form.fields.email.touched
form.fields.email.isDirty
form.fields.email.isValid
form.fields.email.disabled
```

Connecting inputs:

```tsx
<input
  value={form.fields.email.value as string}
  onChange={(e) => form.setValue("email", e.target.value)}
  onBlur={() => form.setTouched("email")}
/>

{form.fields.email.error && <p>{form.fields.email.error}</p>}
```

### Full Example

```tsx
import { useForm } from "@hookraft/useform"

function LoginForm() {
  const form = useForm({
    fields: {
      email: {
        value: "",
        rules: [
          { required: true, message: "Email is required" },
          { pattern: /^\S+@\S+\.\S+$/, message: "Enter a valid email" },
        ],
      },
      password: {
        value: "",
        rules: [
          { required: true, message: "Password is required" },
          { minLength: 8, message: "Must be at least 8 characters" },
        ],
      },
    },
    validateOn: "blur",
    onSubmit: async (values) => {
      await fetch("/api/login", {
        method: "POST",
        body: JSON.stringify(values),
      })
    },
  })

  return (
    <form onSubmit={form.handleSubmit}>
      <input
        value={form.fields.email.value as string}
        onChange={(e) => form.setValue("email", e.target.value)}
        onBlur={() => form.setTouched("email")}
      />
      {form.fields.email.error && <p>{form.fields.email.error}</p>}

      <input
        type="password"
        value={form.fields.password.value as string}
        onChange={(e) => form.setValue("password", e.target.value)}
        onBlur={() => form.setTouched("password")}
      />
      {form.fields.password.error && <p>{form.fields.password.error}</p>}

      <button type="submit" disabled={form.isSubmitting}>
        {form.isSubmitting ? "Logging in..." : "Login"}
      </button>
    </form>
  )
}
```

### Reset Example

Use a ref to call `reset()` from inside `onSuccess`:

```tsx
import { useRef } from "react"
import { useForm } from "@hookraft/useform"

function ContactForm() {
  const formRef = useRef<useForm.Return<any>>(null!)

  const form = useForm({
    fields: {
      name: { value: "" },
      email: { value: "" },
      message: { value: "" },
    },
    onSubmit: async () => {},
    onSuccess: () => {
      formRef.current.reset()
    },
  })

  formRef.current = form

  return (
    <form onSubmit={form.handleSubmit}>
      <input
        value={form.fields.name.value as string}
        onChange={(e) => form.setValue("name", e.target.value)}
      />
    </form>
  )
}
```

### Validation Rules

```tsx
rules: [
  { required: true, message: "Required" },
  { minLength: 3, message: "Too short" },
  { maxLength: 50, message: "Too long" },
  { min: 0, message: "Must be positive" },
  { max: 100, message: "Must be 100 or less" },
  { pattern: /^\S+@\S+\.\S+$/, message: "Invalid email" },
  {
    validate: (value, allValues) => {
      if (value === allValues.username) return "Cannot match username"
      return true
    },
  },
]
```

### Options

<PropsTable
  data={[
    { name: "fields", type: "useForm.FieldsConfig", nameDetails: "Field definitions with initial values, rules, and disabled state" },
    { name: "onSubmit", type: "(values: useForm.FormValues) => Promise<void> | void", nameDetails: "Called when the form is submitted and all fields are valid" },
    { name: "onSuccess", type: "(values: useForm.FormValues) => void", nameDetails: "Fires after onSubmit resolves successfully" },
    { name: "onError", type: "(error: unknown, values: useForm.FormValues) => void", nameDetails: "Fires if onSubmit throws" },
    { name: "onChange", type: "(values: useForm.FormValues, fieldName: string) => void", nameDetails: "Fires on every field value change" },
    { name: "validateOn", type: "\"change\" | \"blur\" | \"submit\"", default: "\"blur\"", nameDetails: "When to run field validation" },
  ]}
/>

### Returns

<PropsTable
  data={[
    { name: "fields", type: "{ [K in keyof T]: useForm.FieldState }", nameDetails: "All field states keyed by field name" },
    { name: "values", type: "useForm.FormValues", nameDetails: "Current value of every field" },
    { name: "errors", type: "useForm.FormErrors", nameDetails: "Current error of every field" },
    { name: "status", type: "useForm.Status", nameDetails: "Current form state: idle | submitting | success | error" },
    { name: "is(status)", type: "(s: useForm.Status) => boolean", nameDetails: "Check the current form status" },
    { name: "isValid", type: "boolean", nameDetails: "True if all fields pass validation" },
    { name: "isDirty", type: "boolean", nameDetails: "True if any field value differs from its initial value" },
    { name: "isSubmitting", type: "boolean", nameDetails: "True while onSubmit is running" },
    { name: "isSuccess", type: "boolean", nameDetails: "True after onSubmit resolves successfully" },
    { name: "isError", type: "boolean", nameDetails: "True if onSubmit threw an error" },
    { name: "submitError", type: "unknown", nameDetails: "The error thrown by onSubmit if it failed" },
    { name: "setValue(name, value)", type: "(name: keyof T, value: useForm.FieldValue) => void", nameDetails: "Update a field's value" },
    { name: "setTouched(name)", type: "(name: keyof T) => void", nameDetails: "Mark a field as touched — triggers blur validation if validateOn is \"blur\"" },
    { name: "setError(name, error)", type: "(name: keyof T, error: string) => void", nameDetails: "Manually set an error on a field" },
    { name: "clearError(name)", type: "(name: keyof T) => void", nameDetails: "Clear the error on a field" },
    { name: "validateField(name)", type: "(name: keyof T) => boolean", nameDetails: "Validate a single field and return whether it passed" },
    { name: "validateAll()", type: "() => boolean", nameDetails: "Validate all fields and return whether the form is valid" },
    { name: "submit()", type: "() => Promise<void>", nameDetails: "Programmatically submit the form without a DOM event" },
    { name: "handleSubmit(e?)", type: "(e?: React.FormEvent) => Promise<void>", nameDetails: "Pass directly to a form's onSubmit — calls e.preventDefault() automatically" },
    { name: "reset()", type: "() => void", nameDetails: "Reset all fields to their initial values and clear all errors and status" },
    { name: "resetField(name)", type: "(name: keyof T) => void", nameDetails: "Reset a single field to its initial value" },
  ]}
/>